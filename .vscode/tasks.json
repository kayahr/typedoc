{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run unit tests",
            "command": "${command:testing.coverageAll}",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
